ktor:
  application:
    modules:
      - dev.gamification.backend.ApplicationKt.module
  deployment:
    port: 8080
app:
  seed:
    enabled: true
  auth:
    jwt:
      secret: dev-only-secret-change-me
  database:
    sqlite:
      url: "jdbc:sqlite:./kls_database.db?foreign_keys=on"
      enabled: true
    mysql:
      enabled: false
      url: "jdbc:mysql://localhost:3306/learning_gamification?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC"
      user: "gamification_app"
      password: "devpwd"
