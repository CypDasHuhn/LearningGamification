ktor:
  application:
    modules:
      - dev.gamification.backend.ApplicationKt.module
  deployment:
    port: 8080
app:
  seed:
    enabled: true
  auth:
    jwt:
      secret: dev-only-secret-change-me
