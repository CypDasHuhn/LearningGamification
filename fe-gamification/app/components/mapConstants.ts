/** Total width of the scrollable map canvas in pixels. */
export const MAP_WIDTH = 1840;

/** Total height of the map canvas in pixels. */
export const MAP_HEIGHT = 430;

/** Radius in pixels of each level/chapter platform node on the map. */
export const NODE_RADIUS = 36;

/** Number of path-sample indices the character advances per animation frame. */
export const CHARACTER_WALK_SPEED = 4;

/**
 * Vertical offset in pixels applied to the character sprite relative to the
 * path sample y-coordinate. Negative value positions the sprite above the path
 * so the character appears to walk on top of it rather than through it.
 */
export const CHARACTER_VERTICAL_OFFSET = -54;
